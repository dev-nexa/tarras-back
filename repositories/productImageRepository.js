const db = require('../config/db');

const productImageRepository = {
    
    getAllImages: async () => {
        try {

      } catch (error) {

      }
    },

    getImageById: async (id) => {
        try {

      } catch (error) {

      }
    },

    createImage: async (imageData) => {
        try {

      } catch (error) {

      }
    },

    updateImageById: async (id, imageData) => {
        try {

      } catch (error) {

      }
    },

    deleteImageById: async (id) => {
        try {

      } catch (error) {

      }
    },

    getImagesByProductId: async (productId) => {
        try {

      } catch (error) {

      }
    }
};

module.exports = productImageRepository;
