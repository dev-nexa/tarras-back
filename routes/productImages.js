const express = require('express');
const router = express.Router();

// router.get('/product-images', productImageController.getAllImages);

// router.get('/product-images/:id', productImageController.getImageById);

// router.post('/product-images', productImageController.createImage);

// router.put('/product-images/:id', productImageController.updateImageById);

// router.delete('/product-images/:id', productImageController.deleteImageById);

// router.get('/product-images/product/:productId', productImageController.getImagesByProductId);

module.exports = router;
