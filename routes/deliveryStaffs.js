const express = require('express');
const router = express.Router();

// router.get('/delivery-staff', deliveryStaffController.getAllStaff);

// router.get('/delivery-staff/:id', deliveryStaffController.getStaffById);

// router.post('/delivery-staff', deliveryStaffController.createStaff);

// router.put('/delivery-staff/:id', deliveryStaffController.updateStaffById);

// router.delete('/delivery-staff/:id', deliveryStaffController.deleteStaffById);

// router.get('/delivery-staff/available', deliveryStaffController.getAvailableStaff);

module.exports = router;
